@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# If you want to require that downstream also find Eigen3, DeePCpp, etc:
# find_dependency(Eigen3)
# find_dependency(DeePCpp)

# Include the exported targets from the library
include("${CMAKE_CURRENT_LIST_DIR}/control_libTargets.cmake")

